% В этом файле мы настраиваем LaTeX под наши цели
% За основу взят предыдущий проект на LaTeX, в котором не было комментов
% (я это к тому, что не стоит доверять комментам здесь)

\ProvidesPackage{style}

\usepackage{latexsym} 
\usepackage[T2A]{fontenc}
\usepackage[utf8]{inputenc} 
\usepackage[russian]{babel} % это всё русские буковки
\usepackage{graphicx} % вставка картинок
\usepackage{array}
\usepackage{ulem} % ??
\usepackage{textcomp}
\usepackage{fancyhdr} % красивый рендеринг страницы
\usepackage{amsmath} % для матана
\usepackage{amssymb}
\usepackage{mathtools}
\usepackage{ntheorem} % для матана
\usepackage{listings} % Листинги с кодом
\usepackage{hyperref} % Ссылочки
\usepackage{amsmath}
\usepackage{dcolumn}   % needed for some tables
\usepackage{bm}        % for math
\usepackage{tikz}
\usepackage[a4paper]{geometry} 
\usepackage{tikzpagenodes}
\usetikzlibrary{arrows}

\newcommand*\circled[1]{\tikz[baseline=(char.base)]{
            \node[shape=circle,draw,inner sep=2pt] (char) {#1};}}

% Настройка листингов с кодом
\lstset{
    basicstyle=\footnotesize\ttfamily,
    breaklines=true,
    numbers=left,
    tabsize=4,
    texcl=true
}

% Дефайним конструкции
\newtheorem*{mydef}{Определение}
\newtheorem*{theorem}{Теорема}
\newtheorem*{example}{Пример}

% Всякие отсупы
\topmargin=-0.45in
\evensidemargin=0in
\oddsidemargin=0in
\textwidth=6.5in
\textheight=9.5in
\headsep=0.25in 

\linespread{1} % Пробелы между строк
\pagestyle{fancy}
\renewcommand\headrulewidth{0.4pt} % хедеры и футеры
\renewcommand\footrulewidth{0.4pt}

\setlength\parindent{0pt} % Удаляем все отступы между абзацами (имхо так лучше)
\newcommand{\fixheader}[1] {
    \fancyhf{}
    \fancyhead[RO,LE]{\MakeUppercase{#1}}
    \fancyhead[RE,LO]{\MakeUppercase{\chaptername}\ \thechapter}
}

\author{Владимир Милосердов, Владимир Шабанов, Шумилов Пётр}
\title{Конспекты по матанализу}